#include "vertex.h"
class edge{
    private:
        double weight;
        vertex starting_vertex;
        vertex ending_vertex;
    public:
        edge();
        edge(double,vertex,vertex);
        edge()
};